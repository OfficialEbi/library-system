# 📘 سامانه مدیریت کتابخانه دانشگاه ابن‌حسام  
یک سامانه کامل برای مدیریت کتاب‌ها، اعضا، امانت‌ها و عملیات کتابخانه  
پیاده‌سازی با **Node.js + Express + MySQL** و فرانت ساده HTML/CSS/JS
دانشگاه ابن‌حسام – درس سیستم‌های مبتنی بر وب
1. معرفی پروژه

 هدف پوشش سه نقش اصلی:

کاربر عادی (Member)

کاربر در انتظار تایید (Pending)

مدیر سیستم (Admin)

این سامانه قابلیت مدیریت کتاب‌ها، اعضا، امانت‌ها (در حال توسعه)، و کنترل دسترسی مبتنی بر نقش را فراهم می‌کند.
پروژه شامل یک Frontend کامل (HTML/CSS/JS) و یک Backend مبتنی بر Node.js + Express + MySQL است.

2. ویژگی‌های پروژه
قابلیت‌های مدیر (Admin Panel)

✔ افزودن کتاب جدید
✔ حذف و ویرایش کتاب
✔ مشاهده لیست کامل کتاب‌ها
✔ مشاهده آمار داشبورد
✔ ثبت عضو جدید
✔ ویرایش اطلاعات اعضا
✔ حذف اعضا
✔ تایید کاربران Pending
✔ دسترسی کامل به تمام APIها

قابلیت‌های عضو تایید شده (Member Panel)

✔ مشاهده تاریخ انقضا حساب
✔ مشاهده لیست امانت‌های خود
✔ ویرایش اطلاعات حساب
✔ Borrow کتاب از صفحه اصلی (در نسخه بعدی فعال می‌شود)

قابلیت‌های عضو در حالت Pending

✔ مشاهده کتاب‌ها
✔ Borrow غیرفعال است
✔ پیام انتظار تایید به صورت ثابت نمایش داده می‌شود
✔ پس از تایید مدیر → تبدیل به Member

3. تکنولوژی‌های استفاده شده
Frontend

HTML5

CSS3

JavaScript (Vanilla)

Layout Responsive

LocalStorage مدیریت سشن

Backend

Node.js

Express.js

JWT Authentication

bcrypt جهت Hash رمز عبور

MySQL + mysql2/promise

CORS

dotenv

4. نحوه اجرای پروژه
1) نصب پکیج‌ها
npm install

2) ساخت فایل .env
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=library
DB_PORT=3308
JWT_SECRET=secret
JWT_EXPIRES_IN=2h

3) ساخت دیتابیس MySQL
CREATE DATABASE library;

4) اجرای سرور
node server.js

5) اجرای Frontend

فایل‌های HTML را فقط با مرورگر باز کنید.

5. نقش‌ها در سیستم
نقش	توضیحات
Admin	دسترسی کامل به مدیریت کتاب، اعضا، آمار
Member	امانت کتاب + پروفایل + لیست امانت
Pending	فقط مشاهده کتاب + Borrow غیرفعال
6. ساختار صفحات Frontend
فایل	توضیح
index.html	صفحه اصلی – نمایش کتاب‌ها – Borrow – پیام Pending
admin-panel.html	داشبورد مدیر – مدیریت کامل سیستم
member-panel.html	داشبورد عضو – پروفایل و لیست امانت‌ها
style.css	استایل کل پروژه
7. ساختار APIهای Backend
Authentication
متد	مسیر	توضیح
POST	/api/signup	ثبت‌نام کاربر – نقش اولیه pending
POST	/api/login	ورود – ایجاد JWT
Books (Admin Only)
متد	مسیر	توضیح
GET	/api/books	دریافت لیست کامل کتاب‌ها
POST	/api/books	افزودن کتاب جدید
PUT	/api/books/:id	ویرایش کتاب
DELETE	/api/books/:id	حذف کتاب
Books (Public)
متد	مسیر	توضیح
GET	/api/public/books	لیست کتاب‌ها برای همه (بدون توکن)
Users (Admin Only)
متد	مسیر	توضیح
GET	/api/users	دریافت لیست همه اعضا
PUT	/api/users/:id/approve	تایید کاربر Pending
POST	/api/admin/users	ثبت عضو جدید توسط مدیر
PUT	/api/admin/users/:id	ویرایش اطلاعات عضو
DELETE	/api/admin/users/:id	حذف عضو
Stats (Admin Only)
مسیر	توضیح
/api/stats/books	تعداد کتاب‌ها
/api/stats/users	تعداد کاربران تایید شده
/api/stats/borrows	تعداد امانت‌های فعال
8. تست دستی (طبق خواسته استاد)
1) تست ثبت‌نام

وارد index.html شوید

ثبت‌نام کنید

پیام Pending نمایش داده می‌شود

Borrow غیرفعال است

2) تست مدیر

وارد اکانت Admin شوید

مدیریت کتاب‌ها → افزودن کتاب → موفق

مدیریت اعضا → مشاهده Pending → تایید → موفق

ویرایش عضو و حذف عضو → موفق

داشبورد → آمار درست نمایش داده می‌شود

3) تست عضو تایید شده

Borrow فعال می‌شود

پروفایل قابل ویرایش است

لیست امانت‌ها نمایش می‌شود (در نسخه بعدی تکمیل)

4) تست دسترسی‌ها

Pending نمی‌تواند وارد بخش‌های Admin شود

Member نمی‌تواند وارد Admin شود

هر مسیر Admin با JWT محافظت شده